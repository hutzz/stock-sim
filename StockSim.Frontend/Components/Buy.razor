@using StockSim.Frontend.Models
@using StockSim.Frontend.Services
@using Microsoft.AspNetCore.Authorization
@using Microsoft.AspNetCore.Components
@inject NavigationManager NavigationManager
@inject ITokenService TokenService
@inject IStockService StockService

<div>
    <EditForm Model="@stock" OnValidSubmit="Valid" OnInvalidSubmit="Invalid" class="buy-form">
        <DataAnnotationsValidator />
        <div class="buy-input-number">
            <InputNumber class="buy-input" id="quantity" @bind-Value="_quantity" required />
        </div>
        <input id="buy-submit" type="submit" value="BUY" />
        <ValidationMessage id="buy-validation-message" For="() => stock.Quantity" />
    </EditForm>
</div>

<style>
    .buy-form {
        margin-top: 1%;
        display: flex;
        align-items: center;
        justify-content: left;
        width: 50vw;
        height: 3vh;
    }

    .buy-input {
        box-sizing: border-box;
        border-radius: 16px 16px;
        border: none;
        outline: none;
        width: 20vw;
        height: 3vh;
        background-color: var(--accent-colour-3);
        padding-left: 0.5vw;
        font-size: 0.5vw;
        color: var(--main-font-colour);
        caret-color: var(--main-font-colour);
    }
    
    .buy-input:focus::placeholder {
        color: transparent;
    }

    #buy-validation-message {
        margin-left: 1%;
    }
</style>
